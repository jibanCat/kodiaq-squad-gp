# Versions this basedir was verified against (Python 3.11).
# GPy pins numpy<2 itself; scipy<=1.12 is required by GPy 1.13.
numpy==1.26.4
scipy==1.12.0
GPy==1.13.2
emukit==0.5.1
h5py==3.16.0
# matplotlib is an unavoidable top-level import of lyaemu.gpemulator, which the
# prediction check loads through lyaemu; without it verify_basedir.py cannot
# import the emulator at all.
matplotlib
# pytest is only needed to run the test suite, not to use the basedir.
pytest>=7.0
